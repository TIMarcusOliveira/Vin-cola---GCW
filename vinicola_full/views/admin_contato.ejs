<h1>Editar Contato</h1>
<form method="POST" action="/admin/contato">
  <% conteudo.forEach(c => { %>
    <div>
      <label>Texto (ID <%= c.id %>)</label><br>
      <textarea name="<%= c.id %>"><%= c.texto %></textarea>
    </div>
  <% }) %>
  <button type="submit">Salvar</button>
</form>
